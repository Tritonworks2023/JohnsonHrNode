
    const mongoose = require('mongoose');
    const { Schema } = mongoose;
    
    const getJobAddressSchema = new Schema({
      
    });
    module.exports = mongoose.model('DBSOM_GET_JOBADDRESS', getJobAddressSchema);
    